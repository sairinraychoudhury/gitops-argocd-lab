apiVersion: networking.gke.io/v1
kind: GCPBackendPolicy
metadata:
  name: argocd-server-backend-policy
  namespace: argocd
spec:
  default:
    protocol: HTTPS
  targetRef:
    group: ""
    kind: Service
    name: argocd-server